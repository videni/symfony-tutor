
<form action="{{ path('acme_demo_registration_register') }}" {{ form_enctype(form) }} method="POST" class="acme_demo_registration_register">
    {{ form_widget(form) }}
    <div>
        <input type="submit" value="{{ 'registration.submit'|trans }}" />
    </div>
</form>
